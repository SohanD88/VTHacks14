# Copy to deploy/.env (ignored); never commit the actual key.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
API_PORT=8000
API_BIND_ADDRESS=127.0.0.1
# Exact Vercel URL, without /api or trailing slash. Additional origins may be listed.
SPATIAL_CORS_ORIGINS=["https://your-project.vercel.app"]
