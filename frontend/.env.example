# Same-origin API calls use Vite's proxy during development/preview.
VITE_API_BASE_URL=/api
API_PROXY_TARGET=http://127.0.0.1:8000
# For a separately hosted API, use its full URL, e.g. https://api.example.com/api.
# VITE_* variables are public browser configuration. Never put secrets here.
