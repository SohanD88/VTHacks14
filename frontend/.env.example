# Local development uses /api and Vite's API_PROXY_TARGET.
# For Vercel, set this in project settings BEFORE building; include /api.
# This value is public. Never put Gemini credentials in any VITE_* variable.
VITE_API_BASE_URL=/api
# API_PROXY_TARGET=http://127.0.0.1:8000
